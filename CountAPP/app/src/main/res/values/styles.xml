<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
         <!--Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="Receitas" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/receitas</item>
        <item name="colorPrimaryDark">@color/receitasForte</item>
        <item name="colorAccent">@color/receitas</item>
    </style>

    <style name="Despesas" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/despesas</item>
        <item name="colorPrimaryDark">@color/despesasForte</item>
        <item name="colorAccent">@color/despesas</item>
    </style>

    <style name="ReceitasWidth" parent="Base.Theme.AppCompat.Light.Dialog.MinWidth">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/receitas</item>
        <item name="colorPrimaryDark">@color/receitasForte</item>
        <item name="colorAccent">@color/receitas</item>
    </style>

    <style name="DespesasWidth" parent="Base.Theme.AppCompat.Light.Dialog.MinWidth">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/despesas</item>
        <item name="colorPrimaryDark">@color/despesasForte</item>
        <item name="colorAccent">@color/despesas</item>
    </style>

    <style name="Login" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/login1</item>
        <item name="colorPrimaryDark">@color/login2</item>
        <item name="colorAccent">@color/login1</item>
    </style>

    <style name="SplashScreen" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryDark">@color/black</item>
        <item name="colorAccent">@color/black</item>
    </style>

    <style name="CalenderViewCustom" parent="Theme.AppCompat">
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="CalenderViewDateCustomText" parent="android:TextAppearance.DeviceDefault.Small">
        <item name="android:textColor">@color/colorPrimaryDark</item>
    </style>

    <style name="CalenderViewWeekCustomText" parent="android:TextAppearance.DeviceDefault.Small">
        <item name="android:textColor">@color/colorPrimaryDark</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
